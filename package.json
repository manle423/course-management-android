{
  "scripts": {
    "format": "prettier --write --single-quote --trailing-comma all --tab-width 2 'server/**/*.{js,scss,json}'",
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "prepare": "husky install"
  },
  "lint-staged": {
    "src/**/*.{js,scss,json}": "prettier --write --single-quote --trailing-comma all --tab-width 2"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^15.2.2",
    "nodemon": "^3.1.0",
    "prettier": "^3.2.5"
  }
}
